# Python (figure generation + invariant checks)
numpy>=1.24
matplotlib>=3.7
# optional (page counting in run_all report)
pypdf>=3.0
# LaTeX (manuscript compilation) — system packages, not pip:
# texlive-latex-base texlive-latex-recommended texlive-latex-extra texlive-fonts-recommended
