# P12AB — consolidated remaining-blocker matrix (human-readable)

**Phase:** P12AB (remaining-blocker forensic audit + P13 entry decision)
**Date:** 2026-09-24 · **Branch:** `phase-1-symbolic`
**Governing specification:** `Paper9_Blueprint v1.5` (amendment A2, PI-directed, P12R)
**Machine-readable twin:** `paper9/audit/P12AB_REMAINING_BLOCKER_MATRIX.json`
**Supersedes:** `paper9/audit/P12AA_PRE_P13_BLOCKER_MATRIX.{json,md}` (retained as the P12AA phase record)

This matrix changes no gate definition, threshold, validation route, benchmark set or numerical result.
It is an audit product: nothing below is closed, promoted or waived.

## Matrix

| Blocker | Evidence available | Governing rule | Exact missing item | Who/what supplies it | Can close now? | Next legitimate action |
|---|---|---|---|---|---|---|
| **B2** — Layer 2a gradient benchmark (Li 2024, Fig. 2(b)) | Solver side complete (genuine bands; Level 1/2 identities at machine precision; three admissible interpretations run at dps 50/536/52131; interpretation registry `p11d_b2_gap_registry.json`); source graph + the paper's own equations | v1.5 **A2.1(b)** (graph plus the material/geometric/normalisation information needed to reconstruct the case); **A2.4** (the `l` vs `l̄` ambiguity "must be resolved from authoritative source information before B2 can be accepted under *either* route"; "B2 may not be reported as validated while the ambiguity stands"); **A2.7** ("parameters ambiguous or insufficient" → Not validated) | The authoritative definition/units of the Fig. 2(b) length scale (`l` dimensional or `l̄ = l/b`, and against which normalising length) and the parameter set actually plotted | the source authors (or a published erratum / released data) — no repository-side step can create it | **NO** | PI chooses Decision A (send the prepared request) or Decision B (formally accept B2 as not validated); no silent choice, no digitisation-derived error |
| **B3** — Layer 2b dipolar-gradient benchmark (Li 2023, Fig. 4(c)) | Formulation identity established (repository layer matrix ≡ source Appendix 3 `[P0][G][P0]⁻¹` to ~1e-16 at 60 digits); pinned run `P11D-B3-R1` (720 points); classical Rytov limit and digitised Fig. 4(b) solid agree; Fig. 4(c) reproduction gives ω̄ ≈ 0.3391 vs the published solid ≈ 0.433–0.436 | v1.5 **A2.2 G1** (reproduce from the source-reported parameters) and **G2** (direct comparison with the published curve); **A2.1(c)** (Not validated when the accompanying information is insufficient); **A2.3** (numerical > graphical > insufficient) | The Fig. 4(c) parameter set and normalisation actually used for that panel (its caption states no values; inheritance from the Fig. 3(b) caption is only inferred), and the source's numerical curve data if a quantitative comparison were ever required | the source authors — the published record contains no numeric values and no supplementary data | **NO** | PI chooses Decision A or B; the residual is documented as **source-side parameter/normalisation insufficiency**, never as a solver or formulation defect |
| **PCR1** | Item 1: B1 satisfied via the graphical route; B2/B3 not validated. Items 2–8: satisfied per the P12C status matrix (subject to the separately authorised A2 manuscript re-tiering edit) | v1.5 **PCR1 item 1**: all mandatory published benchmarks (Layers 1, 2a, 2b) must be validated by an admissible evidence route — quantitative where source numerical values exist, or the labelled graphical route where they do not; "a benchmark that is neither is **not validated** and this item fails" | Validation of **B2 and B3** by one of the two admissible routes | author-data route (external) | **NO** | Resolve B2/B3 first; PCR1 then consumes their states unchanged |
| **G3** | Same as PCR1 item 1; the A2 evidence-row relief applies to a benchmark once it is graphically validated (overlay + documented observables + route classification) | v1.5 **hard gate G3** (line 457): Layers 1, 2a, 2b must each be validated by one of the two admissible routes; "a benchmark met by neither route is not validated and fails this gate"; evidence-in-manuscript rule intact | The same B2/B3 validations, plus (once available) their in-manuscript evidence rows | external (B2/B3); the manuscript block is internal work conditioned on them | **NO** | Close PCR1's items, then build the manuscript evidence block |
| **G4** | Not applicable — G4 is a PI signature over the PCR set | v1.5: "the PI signs G4 only after PCR1–PCR8 are checked against the computation log. A failed PCR blocks submission exactly as G3 does." | A complete PCR1–PCR8 pass set (PCR1 currently fails) | PCR1 closure, then PI signature | **NO** | No independent action exists; G4 is downstream of PCR1/G3 |
| **P5** — production gate | **One** complete production matrix exists (Part A pipeline: studies S1, S3–S9 on the 42-point (θ, AR) design map; `results/raw/p5_production_raw.json`, `git_commit 15814972…`, `param_hash 09dd73f4…`) **and its conventions are the locked/registered ones** (TV4 `N_seg=40` / 121 nodes / 41×81 half-BZ; TV6 Case-H set; TV7 `N=4`; area-preserving `l_iso` scaling). The parallel pipeline record is **pilot-only** (`P5-20260922T200303Z`, 15/15 integrity checks) with production blocked on parameter decisions later locked in favour of the Part A conventions. The manuscript contains **no** P5-production claims | Production-gate requirement recorded in `audit/P5_STATUS.md`; the branch-level gate is CONTESTED and "at most one [S] set and one gate statement can stand" | The **PI/user reconciliation decision** selecting the single production record and gate statement (the evidence basis for that decision already exists in-repo); optionally a common-point numeric cross-validation first | PI/user decision (internal); a cross-validation run, if required, is internal and needs authorisation | **NO** | PI decision, or authorise the specified common-point run (θ = 45°, AR = 3, Case H, `n_mesh = 32`, both solvers, same path nodes) before deciding |
| **R-1** — pipeline-level reproducibility | Property A (solver-level reproducibility) established **for the Route-F configuration only**; property C (estimator-rule protocol determinism, frozen Rule R-fit) established; property B (**governing-artifact reproducibility**) **not established** — the governing artifact is one realization of an unpinned start vector at `tol = 1e-12`; its 32² datum (2.478e-15) is 0.00196× its own reproducibility (1.262e-12) | `P12D`/`P12E`/`P12H` §10: R-1 = pipeline-level reproducibility of the governing artifact; "R-1 remains OPEN. Property B is unachieved and closing it requires the explicitly unauthorized re-baseline"; the estimator-branch closure must never be restated as solver or pipeline determinism | Either (i) an authorised numerical re-run measuring the two-seed/independent reproducibility on the **deployed** configuration, or (ii) an authorised re-baseline under the frozen Rule R-fit | PI authorisation, then an internal run (no author data involved) | **NO** | PI authorises the deployed-configuration measurement (specified in the P12AB audit, Part E) or a re-baseline; **nothing is executed in this phase** |
| **C-1** — 5i criterion | The discriminant predicate set (frozen Rule R-fit: `F = 3` strict, admissible subset {4², 8², 16²}, reproduces the governing rate bit-exactly, verdicts stable across a 273×-wide F window) is validated and applied as the governing admissibility rule (P12G/P12H, A1) | `P12D` C-1: the deployed 5i acceptance is non-discriminant; a discriminant replacement was designed and prototype-tested; `P12H` §14: "C-1 (criterion remains OPEN as a *gate*; the discriminant predicate set is validated, not promoted)" | An explicit PI decision promoting the validated predicate set as the operative 5i gate criterion (or closing C-1 as a gate) | PI decision (internal) | **NO** | Record the decision in a governance record; no numerical change required |
| **P13** — submission phase | All gating evidence produced to date is preserved and traceable; PCR2–PCR8, G1, G2 and G-1 are satisfied; PCR1, G3, G4, P5, R-1 and C-1 are not | Blueprint v1.5 submission rule: "Do not submit" while G3 fails and any PCR fails; the P12-series register additionally carries P5, R-1, C-1 and the author-data decision as open items | **External:** B2/B3 validation. **Internal:** P5 reconciliation, R-1 run-or-decision, C-1 decision, the separately authorised A2 manuscript re-tiering edit, and the PI submission decision | external authors (B2/B3) **and** PI (P5/R-1/C-1/manuscript/submission) | **NO** | PI decides the author-data route and authorises the internal actions; until then P13 remains blocked |

**Not a blocker (context):** **B1** — Layer 1 stands at `GRAPHICAL_VALIDATION` / `PASS` under A2, with the
Rytov analytic match and the labelled overlay; **G-1** — CLOSED; **PCR2–PCR8, G1, G2** — satisfied.

## Reading of the chain

`B2 / B3 → PCR1 → G3 → G4 → (submission)` is acyclic; **P5**, **R-1** and **C-1** are parallel items, not
part of that chain. "Source-limited" is **not** an admissible state: A2.1's three states are exhaustive
and mutually exclusive, and G3/PCR1 fail for any benchmark that reaches neither route. Neither B2 nor B3
can be moved by a repository-side action; P5 and R-1 cannot be moved by author data.

## Next-phase decision (Part J)

**`P13 BLOCKED — BOTH EXTERNAL AND INTERNAL BLOCKERS`.**
P13 cannot start: two of its three mandatory benchmarks are not validated under either admissible route
(external), and three internal items (P5 reconciliation, R-1, C-1) still require an explicit PI
decision or authorisation, with the A2 manuscript re-tiering edit separately unauthorised.
