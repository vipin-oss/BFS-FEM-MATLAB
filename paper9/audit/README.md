# audit/
Traceability matrix (claim -> figure/table -> processed -> raw -> run manifest -> code -> equation -> parameters),
calculation logs, test reports, manifests, checksums, phase completion records.
