# P12F evidence bundle (decision audit only)

| file | content |
|---|---|
| `partA_verify.py` / `partA_verify_output.txt` | Part A/G independent re-verification: errors, four-level fit, CI, residual, P1–P4, run1-vs-run2 determinism, hidden-subset check, trend discrepancy for three solver generations, pairwise rates |

All numbers in `audit/P12F_BPC1_ROUTE_DECISION_AUDIT.md` are recomputed by this script from
`audit/evidence/p12e/*` (controlled runs), the committed P4B JSON/TXT and the Blueprint/plan text.
No solver was run; no artifact was modified.
