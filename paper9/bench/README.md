# bench/
Published benchmark cards (B1-B7), extracted benchmark information, independent reference-method
implementations (transfer matrix), benchmark parameter files, overlay digitisations (plotting ONLY, never for error computation).
